---
title: Engine Architecture
---
**Application**:

* ***Renderer:**
	* swapchain
	* command buffers
	* draw frame

* ***rendering system:**
	* pipeline
	* pipeline layout
	* record command buffer how to render objects

One application, one renderer, multiple rendering system (render in various ways)


Objects / asset has their own properties, not every object compatible to every rendering system.























